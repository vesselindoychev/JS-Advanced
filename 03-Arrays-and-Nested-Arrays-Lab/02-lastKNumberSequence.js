function lastNumbersSequence(n, k) {
    let arr = [];
    // let sum = 0;
    for (let i = 0; i < n - 1; i++) {
        arr.push(0)
    }

    arr.splice(0, 0, 1);

    // for (let times = 0; times < n - 1; times++) {
    //     sum = arr.slice(times + 1, times)
        
    // }

    console.log(arr);
    console.log(Sum)
}

lastNumbersSequence(6, 3)